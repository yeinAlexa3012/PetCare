import { COLORS } from './Colors';

export const appStyle = {
  tabStyle: {
    backgroundColor: '#fff',
    elevation: 10,
    display: 'flex',
    borderTopWidth: 0,
    height: 60,
    position: 'absolute',
  },
};
